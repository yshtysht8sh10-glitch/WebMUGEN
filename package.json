{
  "name": "webmugen",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:public": "tsc && vite build --mode public",
    "build:development": "tsc && vite build --mode development",
    "preview": "vite preview",
    "test": "vitest",
    "matrix:check": "node scripts/compatibility-matrix.mjs",
    "trigger:audit": "node scripts/winmugen-trigger-inventory.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "fflate": "^0.8.3",
    "iconv-lite": "^0.7.3",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "vitest": "latest"
  }
}
